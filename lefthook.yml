pre-commit:
  parallel: true
  commands:
    fmt:
      run: bun run fmt
    lint:
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: bun run lint
    nix-fmt:
      glob: "*.nix"
      run: nixfmt --check {staged_files}
    rust-fmt:
      root: "apps/backend/"
      run: cargo fmt --check
    rust-clippy:
      root: "apps/backend/"
      run: cargo clippy --all-targets -- -D warnings
