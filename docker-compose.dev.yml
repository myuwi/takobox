services:
  caddy:
    image: caddy:2-alpine
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./takobox_data:/data:ro
    ports:
      - 80:80
    extra_hosts:
      - "takobox_web takobox_server:host-gateway"
    environment:
      - BASE_URL=":80"
